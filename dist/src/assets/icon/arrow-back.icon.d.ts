import * as React from "react";
import { ColorValue } from "react-native";
declare const ArrowBackIcon: (props: {
    size?: number;
    color?: ColorValue;
}) => React.JSX.Element;
export { ArrowBackIcon };
